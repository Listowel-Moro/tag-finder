version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "arn:aws:s3:::tag-finder-configs/taskdef.json"
        LoadBalancerInfo:
          ContainerName: "tag-finder-container"
          ContainerPort: 8080
        PlatformVersion: "LATEST"
